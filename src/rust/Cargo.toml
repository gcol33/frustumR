[package]
name = "frustumR"
version = "0.1.0"
edition = "2021"
description = "R bindings for Frustum GPU rendering framework"
license = "MIT"

[lib]
crate-type = ["staticlib"]

[dependencies]
frustum-core = "0.1.0"
frustum-render = "0.1.0"
extendr-api = "0.7"
